name: Tests

on: [push]

jobs:
  dooby:
    runs-on: ubuntu-latest

    steps:
      - name: Install Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.18.x

      - uses: actions/checkout@v2

      - name: Run tests
        run: go test -v
